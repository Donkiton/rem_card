# Матрица реализации клинической аналитики

| Этап | Реализация и критерий | Проверка / доказательство | Состояние |
|---|---|---|---|
| 1. Контракт | `platform/core.py`, `metric_metadata.py`, `graph_catalog.py`, `analytics_clinical_contract.md`; статические формулы, версии, покрытие legacy и графических селекторов, включая точный g23–g45 contract | `python -m pytest -q tests/test_analytics_platform_contract.py tests/test_detailed_statistics_section_units.py` — 11 passed | выполнен технически; клиническое одобрение требуется |
| 2. Derived cases | `StatisticsRepository`, source fingerprint, RAO/OPERBLOCK isolation, half-open periods, earliest valid terminal endpoint, in-memory cohort snapshot и remap multi-DB ID; `ivl_episodes` materialize по пересечению интервала, включая carry-in и identity-safe FK; ambiguous legacy standalone operation admission исключается, explicit/handoff RAO сохраняется | `python -m pytest -q tests/test_analytics_platform_engine.py -k multi_db_carry_in_ivl_episode` — 1 passed; полный регресс ниже | выполнен технически |
| 3. Реестр и engine | `MetricDefinition`, `MetricRegistry`, `AnalyticsEngine`, explicit `PopulationKind` (admission event / interval census / terminal event / recovery / procedure) применяется ко всем KPI/legacy/graph definitions; immutable `GraphMetricArtifact`, fail-fast dispatch g1–g63/recovery, earliest terminal и sweep-line census semantics | `python -m pytest -q tests/test_analytics_platform_engine.py tests/test_analytics_platform_cohorts.py tests/test_analytics_platform_comparison.py` — 75 passed | выполнен технически |
| 4. Когорты | сериализуемые `CohortDefinition`/`CohortFilter`, AND multi-filter, recovery как часть cohort snapshot и параметризованная матрица всех заявленных полей/операторов | `python -m pytest -q tests/test_analytics_platform_engine.py tests/test_analytics_platform_cohorts.py tests/test_analytics_platform_comparison.py` — 75 passed | выполнен технически |
| 5. Страты и сравнение | manual A/B и идентичный предыдущий год без подмены нулём, отдельная manual-empty формулировка | `python -m pytest -q tests/test_analytics_platform_comparison.py` — 2 passed | выполнен технически |
| 6. KPI и виды | KPI definitions, `SavedAnalyticsViewStore` вне clinical DB, explicit schema version / comparison mode / selected metric, backward-compatible full view roundtrip и scope isolation | `python -m pytest -q tests/test_analytics_platform_quality_cache.py tests/test_analytics_platform_ui.py tests/test_archive_center_navigation.py tests/test_archive_query_optimizations.py` — 68 passed | выполнен технически |
| 7. Методика / cases | `MetricResult.explanation`, inclusion reason, distinct source names, `AnalyticsWorkspace` with full registry selector; temporary cohort manager закрывается в finally и при ошибке builder | `python -m pytest -q tests/test_analytics_platform_quality_cache.py tests/test_analytics_platform_ui.py tests/test_archive_center_navigation.py tests/test_archive_query_optimizations.py` — 68 passed | выполнен технически |
| 8. Legacy integration | structured public snapshot methods для detailed/operblock/graphs, registry links legacy keys, same memory snapshot for cohort report/graph; `GraphsBuildResult`, public graph HTML и PDF HTML получают authoritative artifact, A/B выводит summaries артефактов; PDF HTML visibly carries canonical period/cohort/comparison/registry methodology/fingerprint context; public renderer preserves pie/step/histogram/dynamic ward distribution and real Kaplan–Meier time coordinates without SQL recalculation | `python -m pytest -q tests/test_analytics_platform_contract.py tests/test_analytics_platform_ui.py tests/test_archive_center_navigation.py tests/test_analytics_half_open_ranges.py` — covered by full regression below | выполнен технически |
| 9. Качество и кэш | `DataQualityReport`, bounded `SnapshotCache`, fingerprint key, duplicate/time/personnel checks | `python -m pytest -q tests/test_analytics_platform_quality_cache.py tests/test_archive_query_optimizations.py` — 11 passed | выполнен технически |
| 10. UI и регрессия | one unified «Аналитика» destination with RAO/operblock switch and clinical scenario: «Кого анализируем → KPI → Результат / Как рассчитано / Какие пациенты вошли»; legacy statistics retains full height, manual-B progressive disclosure, visible recovery control, multi-filter/saved views, loaded RAO view common graph sync, dynamic production grid g63 for 7+ wards, exhaustive g23–g45 renderer dispatch, фоновые KPI/A/B calculations, stale-result guards, non-GUI Agg graph rendering, sparse temporal axes и offscreen geometry suite at 1280×720 and 1024×640 | `python -m pytest -q tests/test_analytics_platform_quality_cache.py tests/test_analytics_platform_ui.py tests/test_archive_center_navigation.py tests/test_archive_query_optimizations.py` — 68 passed | выполнен технически; врачебная валидация показателей не выполнена |

Финальная проверка после этих изменений: `python -m pytest -q` — **743 passed,
9 subtests passed**. Полный safety-regression: `python scripts/regression_safety_checks.py` —
**538/538 passed**, покрытие реестра полное. Проверки качества и архитектурных
ограничений также завершились без ошибок. Дополнительно aggregate-path regression:
`python -m pytest -q tests/test_analytics_platform_engine.py -k multi_db_carry_in_ivl_episode` — **1 passed**. Требуется отдельная пользовательская/врачебная
приёмка клинических формул и представления результатов.
